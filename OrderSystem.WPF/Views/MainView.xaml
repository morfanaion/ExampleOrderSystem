<UserControl x:Class="OrderSystem.WPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderSystem.WPF.Views"
             xmlns:nav="clr-namespace:OrderSystem.WPF.Navigation"
             mc:Ignorable="d" >
    <nav:WindowSettings.Settings>
        <nav:WindowSettings MinWidth="800" MinHeight="600"/>
    </nav:WindowSettings.Settings>
    <TabControl ItemsSource="{Binding Tabs}" SelectedIndex="0"
                 HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
        <TabControl.ItemTemplate>   
            <DataTemplate>
                <TextBlock Text="{Binding ViewTitle}" />
            </DataTemplate>
        </TabControl.ItemTemplate>
    </TabControl>
</UserControl>
